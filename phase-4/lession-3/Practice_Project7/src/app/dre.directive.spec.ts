import { DreDirective } from './dre.directive';

describe('DreDirective', () => {
  it('should create an instance', () => {
    // const directive = new DreDirective();
    // expect(directive).toBeTruthy();
  });
});
